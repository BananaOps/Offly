apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "offly.fullname" . }}-test-connection"
  labels:
    {{- include "offly.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "offly.fullname" . }}:{{ .Values.service.port }}/status']
  restartPolicy: Never
